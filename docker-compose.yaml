version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8080
    environment:
      - DEBUG=${DEBUG}
    env_file:
      - .env
    ports:
      - "${PORT}:8080"
    restart: unless-stopped
